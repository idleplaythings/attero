var TileDetailsForRaytrace = function TileDetailsForRaytrace(
    position,
    concealment,
    element,
    elevation,
    height,
    unique)
{
    this.position = position;
    this.concealment = concealment;
    this.element = element;
    this.elevation = tileElevation;
    this.elementHeight = height;
    this.unique = unique;
};